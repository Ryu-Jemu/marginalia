---
title: MAP
tagline: 여행 여정 파이프라인입니다. 외부 피드 다섯 개를 게이트웨이 하나 뒤에서 폴링·캐싱하고, 결정적 룰 엔진으로 후보를 거른 뒤, 14노드 에이전트가 여정을 짜고, 자체 호스팅 도로 데이터로 경로를 냅니다.
period: "2026.03 – 현재"
team: 5인 팀
role: 백엔드와 AI — 게이트웨이, 에이전트, 비전, 인프라, 어드민, 그리고 Java BFF
domain: pipeline
status: in-progress
hosting: offline
tier: 1
order: 2
stack:
  - Python
  - FastAPI
  - LangGraph
  - Gemini
  - PostGIS
  - Redis Streams
  - Java 17
  - Spring Boot 3.4
  - STOMP
  - OSRM
  - YOLO
  - Prometheus
  - Grafana
  - Docker
links:
  - kind: deployed
    href: https://map-project-cbd94.web.app
    label: Web build
  - kind: org
    href: https://github.com/we-meet-trip
    label: we-meet-trip
summary:
  - 제가 소유한 서비스 여섯 개 — 게이트웨이, 에이전트, 비전, 인프라, 어드민 콘솔, Java BFF — 그리고 기능 단위로 기여한 Flutter 클라이언트가 있습니다.
  - 요청은 202로 접수하고 스트림으로 답하므로, 느린 모델 호출이 클라이언트 타임아웃이 되지 않습니다.
  - 모델은 룰 엔진이 이미 걸러 둔 후보 중에서 고릅니다. 방문 순서와 이동 구간은 모델에 묻지 않고 좌표에서 계산합니다.
  - 제가 소유한 서비스에 걸쳐 자동화 테스트 981건이 있습니다.
pipeline:
  from: 외부 피드 다섯 개
  to: 휴대폰 위의 여정
  feeds:
    - 날씨와 대기질
    - 장소와 사진
    - 블로그 리뷰
    - 지하철과 공유 모빌리티
    - 도로망
  stages:
    - name: 폴링
      service: hub · scheduler
      store: PostGIS
    - name: 캐시
      service: hub · gateway
      store: Redis L1
    - name: 접수
      service: user · Java BFF
      store: PostgreSQL
    - name: 필터
      service: hub · rule engine
      store: 금지 구역
    - name: 선택
      service: agent · LangGraph
      store: Gemini
      model: true
    - name: 경로
      service: agent · OSRM
      store: 2-opt
    - name: 발행
      service: agent · Streams
      store: job payload
ownership:
  owned:
    - hub — 외부 데이터 게이트웨이, 폴러, 캐시, 룰 엔진
    - agent — 14노드 LangGraph 추천 파이프라인과 계약 검사
    - user — Java BFF, 스트림 컨슈머, 채팅, 재사용 캐시
    - vision — 카메라 인식 서비스, 탐지부터 식별까지
    - infra — compose 토폴로지, 헬스 게이팅, 자체 호스팅 라우팅, Prometheus와 Grafana
    - admin — 운영 콘솔과 내부 API
  notOwned:
    - Flutter 클라이언트 — 소유가 아니라 기능 기여
    - 제품 디자인과 여정 인터페이스
data:
  - value: "6"
    label: 소유한 서비스
    detail: 게이트웨이 · 에이전트 · BFF · 비전 · 인프라 · 어드민
  - value: "981"
    label: 자동화 테스트
    detail: BFF 429 · 게이트웨이 301 · 에이전트 220 · 비전 17 · 어드민 14
  - value: "14"
    label: 에이전트 그래프 노드
    detail: 그중 두 개가 모델을 호출
  - value: "243"
    label: 폴링하는 시군구 격자점
    detail: 전국 예보 커버리지
architecture:
  externalLabel: 외부 피드 다섯 개, 통합 지점 하나
  external:
    - name: KMA — 예보 · 실황
    - name: 대기질 지수
    - name: Kakao — 장소와 사진
    - name: Naver — 블로그 리뷰
    - name: 지하철과 공유 모빌리티
    - name: OSRM — 도로망, 자체 호스팅
  services:
    - name: hub — gateway
      detail: 업스트림 클라이언트 6개 · 캐시 · 룰
      mine: true
    - name: agent
      detail: 14노드 그래프 · 계약 검사
      mine: true
    - name: user — Java BFF
      detail: 접수 · 스트림 · 채팅 · 캐시
      mine: true
    - name: vision
      detail: 카메라 인식 · WebSocket
      mine: true
    - name: admin
      detail: 운영 콘솔 · 내부 API
      mine: true
    - name: infrastructure
      detail: compose · 헬스 게이팅 · 메트릭
      mine: true
  clients:
    - name: Flutter 앱
    - name: 어드민 웹
      mine: true
  stores:
    - PostGIS
    - Redis L1
    - PostgreSQL
    - Redis Streams
verification:
  - surface: user — Java BFF
    count: 429
    detail: 컨트롤러, 서비스, 리포지토리, STOMP, 계약 고정 테스트
  - surface: hub — gateway
    count: 301
    detail: 격자 선택, 폴링, 룰, 가드, 모든 업스트림 클라이언트
  - surface: agent — LangGraph
    count: 220
    detail: 그래프 라우팅, 출력 검증, 인젝션, 타임라인, 레이트 리밋
  - surface: vision
    count: 17
  - surface: admin
    count: 14
safety:
  - title: 종료 상태는 되돌아가지 않습니다
    detail: >-
      접수 쓰기는 행이 없을 때만 일어나고, 그 경로에서의 기본키 충돌은
      업데이트로 재시도하지 않고 무시합니다. 도착 순서 역전, 중복 키, 실패하는
      재시도를 회귀 테스트 세 건이 고정합니다.
  - title: 모델 호출은 세 번으로 제한되고, 두 번을 씁니다
    detail: >-
      선택과 근거 생성이 각각 한 번입니다. 방문 순서와 이동 구간은 좌표에서
      계산합니다. 세 번째 호출은 업스트림의 형식 위반을 요청 실패로 끝내지 않고
      한 번 교정할 수 있도록 남겨 둡니다.
  - title: 출력은 누구에게 보이기 전에 계약으로 검사합니다
    detail: >-
      방문 순서는 선택된 장소의 순열이어야 하고, 구간은 끝에서 끝까지 이어져야
      하며, 모든 좌표는 국내 위경도 범위 안에 들어와야 합니다. 같은 범위 검사가
      게이트웨이와 에이전트 양쪽에 있어 둘이 어긋날 수 없습니다.
  - title: 어느 노드의 오류든 페이로드로 단락됩니다
    detail: >-
      오류를 설정한 노드는 그래프의 나머지를 건너뛰고 곧장 페이로드 구성으로
      갑니다. 발행 노드는 항상 실행되므로, 실패한 작업은 바뀌지 않는 행을
      클라이언트가 계속 폴링하게 두는 대신 실패 이벤트를 냅니다.
  - title: 피드 하나가 빠져도 실패하지 않습니다
    detail: >-
      응답하지 않는 업스트림은 5xx가 아니라 여정 위의 경고를 만듭니다.
  - title: 재추천 상한은 날짜 경계에서 풀립니다
    detail: >-
      여정당 하루 세 번이며, Redis Lua INCR과 EXPIREAT로 세고 롤링 윈도가 아니라
      한국 시간 자정에 만료합니다.
  - title: 레이트 리밋 키는 호출자가 고를 수 없습니다
    detail: >-
      전달된 주소 헤더는 직접 피어가 설정된 신뢰 프록시 범위 안에 있을 때만
      읽고, 엣지가 항상 덮어쓰는 단일 값 헤더만 신뢰합니다.
  - title: 내부 가드는 닫힌 쪽으로 실패합니다
    detail: >-
      내부 토큰은 바이트로 비교하므로, 비ASCII 헤더는 예외를 던지고 500을
      반환하는 대신 403을 반환합니다.
  - title: 재사용 캐시는 실패를 담지 않습니다
    detail: >-
      성공한 추천만 캐시에 들어가고, 히트 시 그 사본에 현재 작업 id를 다시
      씁니다. 백그라운드 갱신은 크기가 제한된 큐에서 돕니다.
  - title: 좌표는 로그에 남지 않습니다
    detail: >-
      출발지와 목적지가 액세스 로그에 평문으로 기록되고 있었고, 거부 응답은 장소
      이름을 그대로 담고 있었습니다. 둘 다 걷어냈습니다.
fixes:
  - title: 끝난 작업이 "진행 중"으로 되돌아갔습니다
    area: user · Java BFF
    symptom: >-
      추천이 완료되어도 클라이언트는 끝없이 폴링했습니다. 몇 초 전에 끝난 작업의
      행이 in_progress로 남아 있었습니다. 원할 때 재현되지 않았습니다.
    cause: >-
      접수 기록과 완료 기록이 서로 다른 두 경로에서 같은 행에 쓰는 구조였습니다.
      완료 이벤트가 데이터베이스 경합에서 이기면, 뒤늦은 접수 쓰기가 종료 상태를
      덮어썼습니다.
    fix: >-
      접수 쓰기는 행이 없을 때만 일어나고, 그 경로에서의 기본키 충돌은
      업데이트로 재시도하지 않고 무시하므로 종료 상태가 되돌아가지 않습니다.
      문제를 만들던 순서를 회귀 테스트 세 건이 고정합니다.
  - title: 예보 조회가 저장된 적 없는 격자를 요청했습니다
    area: hub · gateway
    symptom: >-
      대부분의 시군구에서 단기예보가 비어 돌아왔습니다. 존재하지 않는 구를
      요청하면 오히려 데이터가 나왔는데, 미스가 광역 단위 경로로 흘러갔기
      때문입니다.
    cause: >-
      예보는 폴러가 구독한 격자점에만 쌓이는데, 조회는 요청한 시군구 자신의
      격자점을 썼습니다. 243개 시군구 격자점 중 구독한 격자와 겹치는 것은
      19개뿐이었습니다.
    fix: >-
      같은 광역 단위 안에서 가장 가까운 활성 격자로 해석하고, 광역 경계는 넘지
      않습니다. 이 규칙은 순수 함수이고 테스트로 고정되어 있으며, 예보 경로와
      실황 경로가 모두 이 함수를 호출합니다.
  - title: 폴러가 조용히 죽어 하루를 비웠습니다
    area: hub · scheduler
    symptom: >-
      예보가 하나도 저장되지 않은 발표 주기가 통째로 있었습니다. 작업이 아무도
      await 하지 않는 백그라운드 태스크로 돌았기 때문에 실패는 흔적을 전혀
      남기지 않았고, 매 주기 같은 지점에서 되풀이되었습니다.
    cause: >-
      격자별 루프 안의 데이터베이스 예외가 루프를 빠져나가, 그 발표 회차의 남은
      격자는 기록되지 않았습니다. 허용된 범주 집합을 벗어난 값 하나가 그 격자의
      트랜잭션 전체를 롤백시켰습니다.
    fix: >-
      격자별 예외 격리, 태스크가 죽을 때의 명시적 로그, 쓰기 전 범주 필터링,
      이미 저장된 것을 판단하는 쿼리를 격자마다가 아니라 한 번만 실행하도록 변경,
      그리고 부팅 시 실행과 수동 실행이 함께 볼 수 있는 중복 가드를 넣었습니다.
  - title: 지역 예보 코드 두 개가 서로 바뀌어 있었습니다
    area: hub · gateway
    symptom: >-
      남부 두 지역의 중기 육상예보가 서로의 전망을 반환하고 있었습니다. 모든
      응답은 형식상 정상이었고, 알아채는 방법은 실제 날씨를 아는 것뿐이었습니다.
    cause: >-
      지역 코드 테이블에 두 식별자가 시드된 시점부터 뒤바뀌어 있었습니다.
    fix: >-
      매핑을 바로잡고 지역 코드 테이블을 테스트로 덮었습니다.
  - title: 레이트 리미터가 호출자에게 자기 카운터를 고르게 했습니다
    area: user · Java BFF · edge proxy
    symptom: >-
      호출자는 같은 엔드포인트에 원하는 만큼 요청을 보낼 수 있었습니다. 제한은
      자리에 있었고 한 번도 걸리지 않았습니다.
    cause: >-
      필터가 직접 발신자가 누구든 상관없이 전달된 주소 헤더를 읽어서, 카운터의
      키가 되는 값이 공격자 통제 아래 있었습니다. 엣지가 위조를 막는다는 주석이
      있었지만, 그 전제는 그보다 앞서 철회되어 있었습니다.
    fix: >-
      직접 피어가 설정된 신뢰 프록시 범위 안에 있을 때만 헤더를 읽고, 덧붙일 수
      있는 헤더가 아니라 엣지가 항상 덮어쓰는 단일 값 헤더를 읽습니다. 그 범위
      밖에서 도착한 요청은 실제 출발 주소로 키를 만듭니다.
  - title: 긴 여정이 도로 형상을 조용히 잃었습니다
    area: user · Java BFF
    symptom: >-
      일정 규모를 넘는 여정은 도로를 따르지 않고 경유지 사이를 직선으로
      그렸습니다. 인터페이스에는 아무것도 드러나지 않았고, 로그에 경고 하나가
      있었을 뿐입니다.
    cause: >-
      게이트웨이는 라우팅 요청당 구간을 최대 20개까지 받는데 BFF는 여정 전체를
      한 번에 보냈고, 그래서 경유지가 22개를 넘는 여정은 통째로 거절되어 초과분만
      아니라 모든 구간이 직선으로 떨어졌습니다.
    fix: >-
      20개 단위로 나눠 보내고 원래 구간 순서로 다시 합치므로, 거절된 묶음은 자기
      구간만큼만 손해입니다. 날짜 경계를 넘는 구간은 아예 요청하지 않으며, 장소
      이름과 좌표를 담고 있던 거절 응답은 로그에서 걷어냈습니다.
  - title: 데이터베이스 재시작 한 번이 추천 작업 한 묶음을 죽였습니다
    area: agent
    symptom: >-
      데이터베이스가 돌아온 뒤 추천 작업이 풀이 쥔 커넥션 수만큼 연속으로
      실패했고, 그 뒤에는 저절로 회복되었습니다.
    cause: >-
      체크포인터 풀이 재시작으로 끊긴 커넥션을 그대로 내주었습니다. 죽은
      커넥션은 첫 쿼리가 실패해야 감지되는데, 그때는 이미 작업이 그 커넥션을
      가져간 뒤였습니다.
    fix: >-
      커넥션을 빌려 오는 시점에 검사합니다. 대가는 체크아웃마다 왕복 한 번입니다.
  - title: 내부 가드가 403이어야 할 자리에서 500을 답했습니다
    area: hub · gateway
    symptom: >-
      비ASCII 내부 토큰을 실은 요청이 가드 안에서 예외를 일으켜, 거부가 아니라
      서버 오류를 반환했습니다.
    cause: >-
      토큰을 문자열로 비교했는데, 그 비교 함수는 비ASCII 피연산자에서 예외를
      던집니다.
    fix: >-
      양쪽을 바이트로 인코딩한 뒤 비교하고, 값이 없거나 다르면 403으로
      거부합니다.
---

import N from '../../../components/note/N.astro';

MAP은 뒤에 에이전트가 있는 게이트웨이입니다. 날씨와 대기질, 장소와 사진, 블로그
리뷰, 지하철과 공유 모빌리티, 도로망 — 외부 피드 다섯 개를 정해진 주기로 폴링해
하나의 서비스 뒤에서 캐싱하고 정규화합니다. 여정 요청은 즉시 접수하고, 스트림
위에서 계획하고, 끝나면 답합니다.<N id="map-ownership" lang="ko" />

빈 벌판에서 장소를 고르라고 하면 모델은 없는 장소를 지어냅니다. 이동 반경,
날씨, 제외 구역으로 결정적 엔진이 이미 걸러 둔 후보 목록을 주면, 최악의 경우도
실재하는 장소를 잘못 고르는 것에 그칩니다.<N id="map-llm-budget" lang="ko" />
방문 순서와 이동 구간은 아예 묻지 않고, 비용이 감당되는 범위에서는 좌표로 전수
계산합니다.<N id="map-tsp" lang="ko" /> 에이전트의 14개 노드 중 모델을 호출하는
것은 두 개입니다.<N id="map-graph" lang="ko" />

그래프는 어디에서 실패하더라도 답이 나오도록 쓰여 있습니다. 오류를 설정한 노드는
나머지를 건너뛰고 곧장 페이로드 구성으로 가며, 발행 노드는 항상 실행되므로
실패한 작업은 영영 바뀌지 않을 행을 클라이언트가 계속 폴링하게 두는 대신 실패
이벤트를 냅니다. 이 동작 뒤에는 자동화 테스트 981건이 있습니다.<N id="map-test-count" lang="ko" />

예보는 폴러가 구독한 격자점에만 쌓이는데 조회 경로는 전혀 다른 집합을 요청하고
있었고, 그래서 대부분의 시군구가 빈 응답을 받는 동안 잘못된 요청은 쓸 만한 답을
받았습니다.<N id="map-grid-coverage" lang="ko" /> 도로 경로 배치에는 호출자가
모르는 상한이 있었고, 그 상한을 넘으면 초과분이 아니라 여정 전체를
잃었습니다.<N id="map-batch-cap" lang="ko" />

그 앞에는 예약된 프리페치 위에 얹힌 Redis 계층이 있어 외부 피드는 누가 묻기 전에
이미 데워져 있고,<N id="map-cache-tiers" lang="ko" /> 재추천 상한은 롤링 윈도가
아니라 현지 시간 자정에 풀립니다.<N id="map-rate-limit" lang="ko" />
